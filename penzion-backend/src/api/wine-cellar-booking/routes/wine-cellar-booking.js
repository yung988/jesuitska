'use strict';

/**
 * wine-cellar-booking router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::wine-cellar-booking.wine-cellar-booking');
