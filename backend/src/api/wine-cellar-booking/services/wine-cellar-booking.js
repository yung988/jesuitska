'use strict';

/**
 * wine-cellar-booking service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::wine-cellar-booking.wine-cellar-booking');
